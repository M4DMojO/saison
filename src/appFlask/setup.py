import setuptools

setuptools.setup(
    name='saison',
    author='la team Avengers',
    description='for later',
    packages=setuptools.find_packages(),
    install_requires=[
        "Pillow==9.1.1"
    ],
)